---
type: Project Implementation Task
title: <T-001 任务标题>
description: <一句话说明完整实现结果>
tags: [implementation-task]
timestamp: <ISO 8601 时间>
task: T-001
entry: <任务入口相对路径#任务与顺序>
tech_design: <技术方案相对路径>
---

# <T-001 任务标题>

任务入口：[任务与顺序](<任务入口相对路径#任务与顺序>)

## 追踪与目标

- 功能：F-001
- 技术决策：<无；或 D-001（内联本任务必须遵守的决策摘要、取舍影响和禁止改变点）>
- 变更：C-001
- 验证交接：V-001（<后续集中测试应证明的行为，不要求开发阶段执行>）
- 实现目标：<按照技术方案完成后的代码能力>
- 完成边界：<本任务做到哪里为止，哪些交给后续任务或测试阶段>

## 上下文与边界

- 当前状态：<来自代码和技术方案的事实>
- 目标状态：<本任务完成后的状态>
- 特别关注点：<用户约束和风险；没有则写“无”>
- 不要做：<范围外内容和不得改变的 D-*>
- 禁止捷径：<本任务不得使用的 mock、stub、fallback、固定返回、样例数据、临时存储、空校验、跳过持久化/失败路径等；没有额外项时写“遵守通用反模拟规则，无额外禁止项”>
- BUGFIX：<非 BUGFIX 写“不适用”；否则写根因、风险和回退方案>

## 文件与契约

- 实现文件：`<真实路径或新增路径>`
- 最小检查入口：`<格式/lint/typecheck/编译/基础测试命令涉及的真实路径或命令>`
- 临时检查文件：<无；或写“可临时创建，执行后删除，不作为产物持久化”>
- 其他资产：<migration、配置、文档、fixture 或无>
- 上游依赖：<T-*、接口、字段、状态和前置条件>
- 本任务提供：<接口、数据、事件、状态或页面行为>
- 下游消费：<任务或模块如何使用本任务产物>
- 相邻接口摘要：<上游提供什么；下游需要什么>

## 实施步骤

1. <第一步：按技术方案修改哪些文件、接口、字段或状态；完成判定>
2. <第二步：接入调用链、数据流或 UI 状态；完成判定>
3. <第三步：补齐失败处理、兼容或持久化；完成判定>
4. <如有第四步>

每一步只能实现技术方案已经确认的内容；发现需要新增行为、改接口或改验证定义时停止并回到责任阶段。

## 最小检查

- 格式检查：`<命令；没有则写“项目无独立格式命令”>`
- 类型/构建检查：`<命令；没有则写“项目无独立类型或构建命令”>`
- 基础测试：`<已有低成本基础测试命令；没有则写“无，交给测试阶段集中覆盖”>`
- 失败判定：<任一命令因本任务修改失败时不得完成；既有失败需说明区分方式>

开发阶段不要求新增持久化测试文件，不执行复杂功能测试、E2E、浏览器检查、前端交互验证或完整回归。

## 代码审查要点

| 编号 | 对照项 | 审查方式 | 不通过判定 |
|---|---|---|---|
| REVIEW-001 | <方案中的关键 C-* 是否落到真实代码> | <直接查看文件/调用链/字段> | <缺失或偏离方案> |
| REVIEW-002 | <禁止捷径是否被遵守> | <搜索 mock/stub/fallback/固定返回等> | <用临时代替真实实现> |
| REVIEW-003 | <数据、状态或错误是否完整传递> | <沿调用链审查> | <任一环节断裂或吞错> |

## 测试阶段交接

- 需集中验证的 `V-*`：<V-001 / V-002>
- 用户或系统入口：<后续测试应从哪里进入>
- 关键成功路径：<需要证明的可观察结果>
- 失败路径：<权限、超时、无数据、兼容或回退场景>
- UI/前端验证：<不涉及；或写后续测试阶段需要验证的页面、状态和交互>
- 回归范围：<邻近功能、旧行为或数据兼容>

本区只定义后续测试责任，不要求执行者在开发阶段完成这些验证。
