---
type: Project Implementation Task
title: <T-001 任务标题>
description: <一句话说明纵向交付结果>
tags: [implementation-task]
timestamp: <ISO 8601 时间>
task: T-001
entry: <任务入口相对路径#任务与顺序>
tech_design: <技术方案相对路径>
execution_status: pending
---

# <T-001 任务标题>

任务入口：[任务与顺序](<任务入口相对路径#任务与顺序>)

## 追踪与目标

- 功能：F-001
- 技术决策：<无；或 D-001（内联本任务必须遵守的决策摘要、取舍影响和禁止改变点）>
- 变更：C-001
- 验证：V-001（<首要验证；如适用写最终闭环验证>）
- 纵向交付：<完成后用户或调用者可观察的新行为>
- 独立演示：<不依赖未声明任务的演示方式>

## 上下文与边界

- 当前状态：<来自代码和技术方案的事实>
- 目标状态：<本任务完成后的状态>
- 特别关注点：<用户约束和风险；没有则写“无”>
- 不要做：<范围外内容和不得改变的 D-*>
- 禁止捷径：<本任务不得使用的 mock、stub、fallback、固定返回、样例数据、临时存储、空校验、跳过 UI/持久化/失败路径等；没有额外项时写“遵守通用反模拟规则，无额外禁止项”>
- BUGFIX：<非 BUGFIX 写“不适用”；否则写根因、风险和回退方案>

## 文件与契约

- 测试文件：`<真实路径或新增路径>`
- 实现文件：`<真实路径或新增路径>`
- 其他资产：<fixture、migration、smoke、review 清单或无>
- 上游依赖：<T-*、接口、字段、状态和前置条件>
- 本任务提供：<接口、数据、事件、状态或页面行为>
- 下游消费：<任务或模块如何使用本任务产物>
- 相邻接口摘要：<上游提供什么；下游需要什么>

## 测试设计

- 测试入口：<公共 API、用户入口、稳定契约或验证检查>
- Mock 边界：<允许的外部系统；自有模块不得 mock>
- 既有失败区分：<无既有失败；或说明如何区分既有失败与本任务失败>

| 场景 | 输入或前置 | 动作 | 预期结果 | 类型 | 关联验证 | 测试资产 |
|---|---|---|---|---|---|---|
| <核心场景> | <条件> | <操作> | <外部可观察结果> | 集成/单元/UI/smoke/检查 | V-001 | `<路径>` |

具体测试或检查内容：

```text
<写明测试名称、调用入口、关键输入和具体断言；复杂时提供可直接实现的测试骨架或审查清单。>
```

## 实施步骤

### Cycle 1：<一个可观察行为>

RED 计划：

- 测试或检查资产：`<路径>`
- 命令：`<精确命令>`
- 预期失败：<只因为目标行为尚未实现>

GREEN 计划：

- 最小实现：<文件、函数、接口、字段、状态或交互>
- 命令：`<与 RED 相同的命令>`
- 预期通过：<外部可观察结果>

REFACTOR 计划：

- 范围：<必要整理或无>
- 回归：`<当前及相关既有测试命令>`

## 执行断言

| 编号 | 类型 | 断言 | 检查方式 | 失败判定 |
|---|---|---|---|---|
| ASSERT-001 | 必须消失 | <旧调用、字段、分支或文件> | `<rg 或等价命令>` | <仍存在时不得完成> |
| ASSERT-002 | 必须出现 | <新接口、字段、日志或测试资产> | `<命令>` | <不存在时不得完成> |
| ASSERT-003 | 必须传递 | <字段或状态的完整链路> | <代码与运行证据> | <任一落点缺失时不得完成> |
| ASSERT-004 | 必须落盘 | `<精确资产路径>` | `<文件检查和运行命令>` | <缺失或未运行时不得完成> |

## 完成与证据要求

- 完成条件：<精确行为和范围>
- 可观察结果：<完成后外部能观察到的行为或状态>
- 独立演示：<谁通过什么入口演示，不依赖未声明任务>
- 必须运行：<当前任务测试、邻近回归、独立演示>
- 失败路径：<适用失败、权限、超时、兼容或回退场景>
- 证据要求：<RED、GREEN、REFACTOR、V-*、演示和执行断言的证据>
- 禁止完成：<not_verified、blocked、模拟实现或未满足断言时不得 done>

## 执行记录

> 本区由 `plan-execution-v3` 填写，不得改写以上规划契约。

- 状态：pending
- 修改摘要：<待执行>
- RED / GREEN / REFACTOR：<待执行；完成后链接证据>
- 验证与演示：<待执行；完成后链接 V-*、失败路径和回归证据>
- 执行断言：<待执行；完成后记录结果和证据>
- 偏差与阻塞：<无；或记录发现、影响和回退阶段>
