---
name: implementation-planning
description: |
  实施计划创建skill。将技术方案拆分为可执行的任务清单，每个任务有顺序、依赖、验收标准。
  聚焦任务编排，通过链接引用技术文档，避免内容重复和不一致。
  输出目录：workplace/v{N}/plan/
  触发词：实施计划、任务清单、工作项拆分、开始实现。
---

# 实施计划创建

将技术方案拆分为任务清单，通过链接引用已有文档。

<HARD-GATE>
技术方案必须已存在。实施计划不重复技术细节，只编排任务顺序。
</HARD-GATE>

## 核心设计

输出文档分两层，解决**上下文占用**和**状态追踪**两个问题：

- **执行索引**：紧凑任务表，含状态列。每次执行只需读这一节，找到下一个"待执行"任务
- **任务详情**：每个任务独立成节。执行某任务时只读对应章节，不加载其他任务内容

## 工作流程

1. 从 `workplace/v{N}/tech-design/` 读取技术方案
2. 提取技术方案中的架构设计、数据模型、API 设计，自主拆分阶段和任务
3. 确认执行顺序：数据层 → 服务层 → 接口层 → 前端
4. 关联验收标准（来自技术方案的测试策略、约束定义）
5. 生成下方模板格式的文档，提交用户确认

**检查**：若无技术方案，提示用户先完成技术方案设计。

---

## 输出模板

文件路径：`workplace/v{N}/plan/YYYY-MM-DD-{项目名}-任务清单.md`

~~~markdown
# {项目名} 任务清单

**目标**：[一句话说明]
**技术方案**：[技术方案文档路径链接]

---

## 执行索引

> **使用方式**：每次执行前只读这个表，找第一个"待执行"任务。
> 开始任务时将状态改为"执行中"，完成后改为"完成"，然后提交变更。

| 任务 | 描述 | 状态 | 前置 | 参考 |
|------|------|------|------|------|
| T1-1 | [简短描述，10字以内] | 待执行 | 无 | §2.1 |
| T1-2 | [简短描述，10字以内] | 待执行 | T1-1 | §2.2 |
| T2-1 | [简短描述，10字以内] | 待执行 | T1-2 | §3.1 |
| T2-2 | [简短描述，10字以内] | 待执行 | T2-1 | §3.2 |

**状态值**：`待执行` / `执行中` / `完成` / `跳过`

---

## 任务详情

> **使用方式**：执行某任务时，用搜索/跳转定位到对应标题，只读该节内容。

### T1-1: {任务名称}

**目标**：[一句话说清楚做什么]
**前置依赖**：无
**验收**：
- 测试：`pytest tests/xxx -v` → PASS
- 检查：[具体检查点]
- 参考：[技术方案 §2.1 章节名](相对路径)

---

### T1-2: {任务名称}

**目标**：[一句话说清楚做什么]
**前置依赖**：T1-1
**验收**：
- 测试：`pytest tests/xxx -v` → PASS
- 检查：[具体检查点]
- 参考：[技术方案 §2.2 章节名](相对路径)

---

### T2-1: {任务名称}

**目标**：[一句话说清楚做什么]
**前置依赖**：T1-2
**验收**：
- 测试：[测试命令] → [预期结果]
- 参考：[技术方案 §3.1 章节名](相对路径)

---

## 里程碑（可选，任务数>8时使用）

| 里程碑 | 验收点 | 包含任务 |
|--------|--------|----------|
| M1 数据层完成 | [验收描述] | T1-1, T1-2 |
| M2 服务层完成 | [验收描述] | T2-1, T2-2 |
~~~

---

## 设计原则

- **聚焦编排**：不重复技术细节，通过链接引用技术方案章节
- **按需加载**：索引紧凑（O(n)行），详情按章节独立读取（O(1)任务）
- **状态持久**：执行后立即更新索引状态并提交，跨会话可接续
- **验收可执行**：每个任务有具体测试命令或人工检查点
- **粒度适中**：单个任务目标1-2小时可完成

## 特殊情况

- **任务过大**：拆分为子任务（T1-1a, T1-1b），共享同一参考章节
- **无测试命令**：改为"检查：[具体人工验收步骤]"
- **简单任务（<5个）**：跳过里程碑，直接产出精简清单
