---
name: plan-execution-v2
description: 执行已确认的 implementation-planning-v2 任务计划，通过修改代码、运行真实验证、记录证据并拒绝模拟完成来交付实现。当用户要求开始实现、执行计划、按计划写代码或继续已有 v2 计划时使用。
---

# 计划执行 v2

直接实施已确认的任务计划，并用真实证据验证。主 agent 对执行端到端负责。subagent 可以用于审查或调查，但不能替代真实实现和验证。

## 先读取

执行前读取这些引用：

- `../project-development-v2-common/references/no-simulation-policy.md`
- `../project-development-v2-common/references/delivery-contract.md`
- `references/execution-checklist.md`

## 核心规则

- 必须从包含 `index.md` 和 `T-*` 任务文件的 v2 计划目录开始。
- 使用 `index.md` 管理任务顺序和状态。
- 读取当前 `T-*` 文件；有歧义时再读取来源需求和技术方案。
- 默认由主会话直接编辑代码，除非用户明确要求委派。
- subagent 只用于聚焦代码审查、调查或独立验证。
- 只有实现完成且验证证据已记录，任务才能标记完成。
- 不得把 mock、stub、fallback、伪造输出、未检查 UI 或未运行测试当作完成。
- 如果验证无法运行，标记为 `NOT VERIFIED` 并写清阻塞原因。
- 如果计划包含最终闭环/放行门槛，未执行或未通过前不得宣布整体完成。

## 流程

1. 在 `workplace/<version>/plan/` 找到计划目录，或使用用户给定路径。
2. 读取 `index.md`。
3. 选择第一个状态为 `todo` 或 `in_progress` 的任务。
4. 将任务状态更新为 `in_progress`。
5. 读取任务文件和必要上下文。
6. 按正常代码编辑流程实现任务。
7. 运行任务定义的验证命令或步骤。
8. 在任务范围内修复失败。
9. 填写任务文件中的 `证据日志`。
10. 更新 `index.md` 状态：
    - 验证通过才写 `done`。
    - 已实现但无法验证写 `not_verified`。
    - 无法继续实现写 `blocked`。
11. 用户没有要求停止时，继续下一个任务。
12. 所有普通任务结束后，执行最终闭环/放行任务。
13. 运行 `index.md` 中的全局验证并总结剩余风险。

## 证据要求

每个任务记录：

- 修改文件。
- 执行过的命令。
- 关键输出或结果。
- 用户可见任务的 UI/浏览器证据。
- 跳过的验证及阻塞原因。
- 与计划的偏差。

不要粘贴大段日志，只保留能证明验证真实发生的关键信息。

## 处理计划问题

如果计划错误或不完整：

- 不要静默发明新范围。
- 做能继续执行的最小计划修正。
- 在 `index.md` 或任务文件中记录修正。
- 只要不改变产品范围或主要设计决策，可以继续。
- 如果修正影响范围、数据模型、兼容性、安全或预期 UX，必须询问用户。

## 验证标准

使用任务可行范围内最强的验证：

- 有单元、集成或契约测试时运行它们。
- 变更触及共享行为时，运行 build、typecheck 或 lint。
- UI 任务可行时检查真实渲染页面。
- API 任务可行时调用真实本地 handler/server 或运行集成测试。
- 数据迁移可行时，在真实本地/测试数据库验证 schema 或数据影响。

如果验证依赖不可用服务或凭据，说明缺少什么，并标记 `NOT VERIFIED`。

## 完成标准

单任务完成必须同时满足：

- 代码已修改。
- 必要验证已尝试。
- 通过证据已记录，或明确标记 `NOT VERIFIED`。
- 没有用捷径替代计划需求。
- 没有隐藏无关重构或范围扩张。

最终完成必须满足：

- 所有任务为 `done`，或明确说明未完成状态。
- 全局验证已运行，或因阻塞标记 `NOT VERIFIED`。
- 最终闭环/放行门槛已通过，或明确标记 `NOT VERIFIED` 并说明不能整体放行。
- 最终总结把完成情况映射回 `R-*`、`C-*`、`T-*` 和 `V-*`。
