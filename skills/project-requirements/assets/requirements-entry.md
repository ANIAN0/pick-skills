# <需求标题>

<用 2-4 句话直接说明：谁在什么场景遇到什么问题，本需求要让他得到什么结果，以及读者读完本文应能判断什么。不要写文档编号、状态、负责人、审查、变更记录或待确认问题。>

## 路线图节点

`<M-001>`：<从 ROADMAP.md 复制的一句话核心内容>

## 背景与目标

<按“为什么现在要做 -> 现状问题 -> 做好后的业务变化”组织，不按讨论时间线组织。只保留理解需求必需的事实；证据细节、调研过程和讨论记录放到同名支撑目录。>

## 用户与场景

<先讲清目标用户及其关键差异，再列用户故事。多个用户或场景之间有继承、并列、先后关系时，在这里一次讲清，不要把同一背景散落到每个功能点。>

### <场景或用户故事标题>

- **作为** <具体角色，含关键属性>
- **当** <场景 / 触发条件 / 上下文>
- **我需要** <动词驱动的功能或行为>
- **以便** <业务价值或用户可见结果>

<如果有多个用户故事，按读者理解顺序继续列出；不要为了编号而拆出重复故事。>

## 功能需求

<每个功能小节必须就近写完整：能力、关键规则、异常/边界、必要 UI 要求和验收要求。不要把验收、范围限制、异常处理集中放到文档后部。>

### F-001 <功能名称>

<用 1-3 句话说明这个功能解决什么问题、用户如何感知结果、关键规则是什么。范围限制、异常和失败反馈如果只属于本功能，直接写在这里。不要写实现方案、API、数据库、文件路径或任务。>

**验收要求**

- <用户能观察到什么来判断这个功能已正确完成>
- <关键边界：当 X 发生时，用户看到什么结果>
- <关键失败：当 Y 失败时，系统如何反馈或保持什么状态>

### F-002 <功能名称>

<同上。每个功能点都在本小节内写自己的验收要求，不把验收集中放到文档后部。>

**验收要求**

- <可观察验收点>

## UI 要求

<涉及 UI 时，只写理解需求必需的页面、状态、交互、数据、失败反馈和可访问性要求；能归入某个功能点的 UI 要求优先写在对应功能小节。完全不涉及 UI 时写“不涉及 UI”。>

## 补充说明

<只放阅读正文必须知道、但不适合归入前面章节的最终结论，例如全局术语定义或跨功能约束。没有必要内容时删除本节。不要放变更记录、待确认问题、待定决策、审查结论或讨论摘要。>
