# 实施计划模板

## `index.md`

```markdown
# <功能名称> 实施计划

## 来源
- 需求：
- 技术方案：

## 确认记录
| 闸门 | 确认内容 | 用户结论 | 影响 |
|------|----------|----------|------|
| G1 覆盖审计 | <需求/验收/服务/API/数据/UI/验证覆盖> | 已确认/调整/跳过 | <影响> |
| G2 任务拆分 | <任务边界、顺序、依赖> | 已确认/调整/跳过 | <影响> |
| G3 验证和风险 | <验证方式、最终闭环、执行风险> | 已确认/调整/跳过 | <影响> |

## 覆盖审计
| 类别 | 来源项 | 必须覆盖 | 计划落点 | 结果 |
|------|--------|----------|----------|------|
| 需求 | R-001 | <用户能力/系统能力> | T-001 | 覆盖 |
| 验收 | A-001 | <可观察验收口径> | V-001 / T-001 | 覆盖 |
| 服务边界 | <服务/模块> | <职责与物理位置> | T-001 | 覆盖 |
| API | <接口/入口> | <请求/响应/错误> | T-002 | 覆盖 |
| 数据 | <实体/表> | <字段/迁移/追踪来源> | T-003 | 覆盖 |
| 前端页面 | <路由/页面> | <状态/入口/反馈> | T-004 | 覆盖 |
| 运行闭环 | <用户路径或跨服务链路> | <真实串联证据> | T-999 | 覆盖 |

## 架构和范围决策
- 代码落点：
- 服务边界：
- 前端页面范围：
- 前端覆盖声明：<有前端任务 / 本项目无 UI，原因>
- 明确不做：
- 需要用户确认：

## 执行顺序
| 任务 | 标题 | 状态 | 追踪 | 验证 | 执行断言 |
|------|------|------|------|------|----------|
| T-001 | <标题> | todo | R-001, A-001, C-001 | V-001 | <必须消失/出现/传递/落盘数量> |

默认排序理由：
- <数据/迁移 -> 服务/领域 -> API/适配器 -> 前端基础 -> 前端页面 -> 集成/闭环；若不同，说明原因>

## 测试资产规划
根目录：`workplace/<version>/test/`

| 类型 | 目录 | 覆盖内容 | 关联 V-* | 运行命令 |
|------|------|----------|----------|----------|
| backend-unit | `backend/unit/<module>/` | <纯逻辑/校验> | V-001 | `<command>` |
| backend-integration | `backend/integration/<module>/` | <DB/API/MCP/daemon/diagnostics> | V-002 | `<command>` |
| contract | `contract/` | <服务间契约/schema/OpenAPI> | V-003 | `<command>` |
| frontend-unit | `frontend/unit/<page-or-store>/` | <前端状态/mapper/store> | V-004 | `<command>` |
| frontend-component | `frontend/component/<component>/` | <组件渲染和交互> | V-005 | `<command>` |
| smoke | `smoke/` | <最终闭环/UI smoke> | V-999 | `<command>` |
| review | `review/` | <人工或 subagent 审查清单> | <V-*> | <步骤> |
| fixtures | `fixtures/` | <共享测试数据> | <V-*> | 不适用 |

## 最终闭环/放行门槛
- T-999: <端到端或跨模块闭环任务；简单纯后端/纯文档任务可写“不适用”并说明原因>
- 必须证明：
  - 关键用户路径真实串联。
  - 前端和后端字段/状态一致。
  - 数据、日志、追踪或历史事实源可回查。
  - 生产路径无 mock、固定回复、硬编码测试 ID、测试旁路或降级实现冒充完成。

## 可追踪矩阵
| 需求 | 验收 | 变更 | 任务 | 验证 | 执行断言 |
|------|------|------|------|------|----------|
| R-001 | A-001 | C-001 | T-001 | V-001 | ASSERT-001 |

## 全局验证
- <命令或步骤>

## 风险和阻塞
- <风险或阻塞>

## 交接清单
- 计划目录：
- 需求文档：
- 技术方案：
- 任务数量：
- 验证数量：
- 测试资产根目录：
- 前端覆盖：
- 最终闭环任务：
- 特别关注点：
- 进入执行时只读 `index.md`，执行某任务时读取对应 `T-*` 文件。

## 审查记录
| 审查项 | 结果 | 发现 |
|--------|------|------|
| 技术方案阻塞发现已处理 | 通过/问题 | <REV-* 或说明> |
| 任务覆盖所有 C-* 和 V-* | 通过/问题 | <REV-* 或说明> |
| 任务足够细且可执行 | 通过/问题 | <REV-* 或说明> |
| 最终闭环可证明真实串联 | 通过/问题 | <REV-* 或说明> |
| 无 mock/fallback 完成路径 | 通过/问题 | <REV-* 或说明> |
| 每个任务有执行断言和残留扫描 | 通过/问题 | <REV-* 或说明> |
| 测试资产路径精确到文件 | 通过/问题 | <REV-* 或说明> |
```

## `T-001-<name>.md`

```markdown
# T-001: <任务标题>

## 追踪
- 需求：
- 验收：
- 变更：
- 验证：

## 目标
<一句话目标。>

## 文件
- <路径>

## 特别关注点
- <与本任务相关的用户强调约束；没有则写“无”>

## 测试文件
- 单元测试：
- 集成测试：
- 契约测试：
- UI/Smoke：
- Fixtures：
- Review 清单：
- 不新增测试的理由：

## 契约和依赖
- 上游依赖：
- 本任务提供：
- 下游消费：
- 数据/字段契约：

## 相邻接口摘要
- 上游提供：
- 下游需要：
- 字段/事件/状态约束：

## 测试场景
| 场景 | 输入/前置 | 预期结果 | 类型 | 关联 V-* | 测试资产 |
|------|-----------|----------|------|----------|----------|
| <场景> | <输入> | <预期> | 单元/集成/契约/UI smoke/review | V-001 | `<path>` |

## 执行断言
| 编号 | 类型 | 断言 | 检查方式 | 失败判定 |
|------|------|------|----------|----------|
| ASSERT-001 | 必须消失 | `<旧循环/过滤/字段/调用>` 不再出现在 `<path>` | `rg -n "<pattern>" <path>` 应无命中 | 仍有命中则任务不得 done |
| ASSERT-002 | 必须出现 | `<新参数/调用/字段/metadata>` 出现在 `<path>` | `rg -n "<pattern>" <path>` 应命中 | 无命中则任务不得 done |
| ASSERT-003 | 必须传递 | `<field>` 从 `<caller>` 传到 `<callee>` 和 `<event metadata>` | 检查调用链 + 运行/测试证据 | 任一链路缺失则任务不得 done |
| ASSERT-004 | 必须落盘 | 测试资产 `<workplace/<version>/test/.../test_x.py>` 存在且覆盖 V-001 | `Test-Path <path>` + 运行命令 | 文件不存在或只做结构检查则任务不得 done |

## 步骤
1. <具体实现步骤>
2. <具体实现步骤>

## 不要做
- <捷径或范围边界>

## 验证
- V-001:
  - 类型：单元 | 集成 | 契约 | UI smoke | 构建 | 手动可观察
  - 测试资产：`workplace/<version>/test/<type>/<name>/...`
  - 验收内容：
    ```text
    <脚本内容、smoke 步骤或 review 清单内容；不得只写“运行测试”>
    ```
  - 命令或步骤：
  - 预期证据：
  - 失败时处理：

## BUGFIX 补充
- 关联根因：<仅 BUGFIX 计划填写，否则写“不适用”>
- 风险点：<仅 BUGFIX 计划填写，否则写“不适用”>
- 回退方案：<仅 BUGFIX 计划填写，否则写“不适用”>

## 完成标准
- <精确完成条件>
- <真实验证通过，或明确 not_verified 风险并记录阻塞>
- <所有执行断言均通过；必须消失项无残留，必须出现/传递/落盘项有证据>
- <未使用 mock/fallback/stub 冒充完成>

## 证据日志
由 plan-execution-v2 填写。
```
