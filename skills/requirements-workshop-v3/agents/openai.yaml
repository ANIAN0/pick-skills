interface:
  display_name: "需求工作坊 v3"
  short_description: "确认用户故事并生成自适应层级的完整需求文档"
  default_prompt: "使用 $requirements-workshop-v3 先确认整体用户故事，再按真实边界决定是否拆模块和功能点，把需求与内部 F-* 写入完整 requirements.md，并等待 G-REQ。"
