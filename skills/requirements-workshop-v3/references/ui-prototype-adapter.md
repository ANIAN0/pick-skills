# UI 与原型适配

输入必须包含 requirements document ID、内部 `R-*`/`A-*`/可选 `F-*`、`scope_ref`、页面、状态、交互要求和验收边界。

输出保存真实文件路径、生成 Skill、覆盖状态和观察证据，并以完整 UI Spec/Prototype 文档回链 requirements。不得创建 Capability、Verification 或 Evidence 文件节点；生成失败时写入验证报告或 Review Report，不弱化需求。
