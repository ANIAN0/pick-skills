# 改动自检清单

修改任意一个具体 skill 的 SKILL.md 之前和之后，过一遍这份清单。目的是让"改动影响可预期"这件事可执行，而不是停留在原则上。

判定原则的完整说明见 [`skills/skill-meta-protocol/SKILL.md`](skills/skill-meta-protocol/SKILL.md)；表达规范（可预测性、渐进展示、单一权威来源、主导词、规则可执行性测试、借口对照表）见 [`skills/skill-meta-protocol/references/glossary.md`](skills/skill-meta-protocol/references/glossary.md)。

## 改动前

- [ ] 我要改的内容，能用一句话说清楚属于"标准模块清单"里的哪一个模块？（如果说不清楚，说明这次改动本身跨越了模块边界，应该先拆分成多个独立改动）
- [ ] 我打开了 skill-meta-protocol 的"改动影响自检表"，找到了对应行，看清楚了"需要同步检查的地方"。
- [ ] 如果改动涉及状态值命名，我确认了新名字没有跟 skill-meta-protocol 里定义的现有状态值冲突或同义重复。
- [ ] 如果这是个流程型 skill，我确认了这次改动是否会让"已经存在但还在进行中"的存量数据（比如某个 `T-*` 任务、某份入口文档）处于未定义状态。如果会，我已经想清楚迁移规则，不是留给运行时随机处理。
- [ ] 如果改动涉及跨 skill 引用，我已经列出了哪些 skill 引用了我要改的内容。
- [ ] 我用 skill-meta-protocol `references/glossary.md` 末尾"把这套规范用在审查现有 skill 时的检查问题"清单过了一遍这段新内容（不在此处复述具体问题，清单本身会随表达规范演进，复述会导致这里过时）。

## 改动中

- [ ] 我只改动了目标模块对应的那个二级标题区块，没有借机"顺手"改动旁边模块的措辞。
- [ ] 我没有在这个模块里写另一个模块该写的内容（比如在"阶段与流转"里写了具体操作步骤）。
- [ ] 如果我发现旧版本里有两个模块重复表达了同一条规则，我把它们合并成一处，并让另一处改成"引用"而不是复制。

## 改动后

- [ ] 我重新通读了一次改动后的模块，确认它仍然只回答自己该回答的那一个问题。
- [ ] 我检查了 skill-meta-protocol 自检表里列出的"需要同步检查的地方"，该改的都改了。
- [ ] 如果改动涉及路径或命名规则，我搜索了仓库里是否有脚本、其他 SKILL.md 或 references 文件硬编码了旧的路径/命名，并同步更新。
- [ ] 如果这是个会被其他 skill 调用的 skill，我重新读了一遍调用方的"触发与边界"模块，确认调用方的描述仍然准确。
- [ ] 我没有为了"以后可能要用"而提前加入空章节或占位说明——没有用到的模块标题已经从文件里删除，不是留空。
- [ ] commit message 里写清楚了"改了哪个模块、为什么改"，而不是泛泛的"优化 skill"。

## 新建 skill 时额外检查

- [ ] 我先判断了这个 skill 属于流程型 / 工具型 / 知识型中的哪一种，并据此从 `skills/skill-meta-protocol/SKILL_template.md` 里只保留对应模块。
- [ ] description 字段一句话写清楚了触发条件，没有写成实现细节的摘要。
- [ ] 如果这个 skill 会被其他 skill 调用，或者它本身要调用别的 skill，我在"触发与边界"或对应模块里用统一格式做了引用（"调用 `<skill-name>`，见其 SKILL.md"），没有复制对方的执行步骤。